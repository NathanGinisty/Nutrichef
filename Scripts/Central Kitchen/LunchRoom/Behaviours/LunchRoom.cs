using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LunchRoom : MonoBehaviour
{
    [SerializeField] Transform posEnterOfRestaurant;

    public Transform PosEnterOfRestaurant { get => posEnterOfRestaurant; set => posEnterOfRestaurant = value; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
